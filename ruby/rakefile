require 'meerpush'

task :default => :create

desc "Create Website"
Website::Create.new do |w|
    w.Name = 'Meerpush_Website'
    w.Home = File.join File.dirname(Dir.getwd), '\meeriis.tests\test_site'
end